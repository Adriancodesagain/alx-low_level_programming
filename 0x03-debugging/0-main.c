#include "main.h"

/**
 * main - Entry point
 *
 * Dscription: program checks whether the interger given is positive or negative.
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int i;
	
	i = 0;
	positive_or_negative(i);

return (0);
}
